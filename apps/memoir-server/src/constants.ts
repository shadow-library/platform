export const APP_NAME = 'memoir-server';

/** The dev/test-only telemetry pseudo-id HMAC key; a production deployment refuses to boot while the secret still equals it (§23). */
export const DEV_PSEUDO_ID_SECRET = 'dev-only-telemetry-pseudo-id-secret';
