{
  "id": "role-route",
  "version": "1.0.0",
  "title": "Role Route",
  "description": "Fixture plugin that raises the writer class for one named call role and leaves every other role alone.",
  "decisionPoints": ["call.route"],
  "exclusive": ["call.route"],
  "forms": {
    "settings": {
      "fields": [{ "name": "raisedRole", "type": "string", "title": "Raised role" }],
      "required": ["raisedRole"]
    }
  }
}
