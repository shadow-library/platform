/**
 * Importing npm packages
 */

/**
 * Importing user defined packages
 */

/**
 * Defining types
 */

/**
 * Declaring the constants
 */

/**
 * The reading column both shells hand to `Shell`, so a page never changes width when an operator
 * crosses between the portal and the console. The shell centres it and supplies the gutters.
 */
export const PAGE_WIDTH = 1040;
