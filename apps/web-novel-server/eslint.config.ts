/**
 * Importing npm packages
 */

/**
 * Importing user defined packages
 */
import { createConfig } from '../../eslint.config.ts';

/**
 * Defining types
 */

/**
 * Declaring the constants
 */

/** Declaration-merged namespaces carry the Fastify request augmentations. */
export default createConfig({ rules: { '@typescript-eslint/no-namespace': 'off' } });
