name: Publish package to NPM
permissions:
  id-token: write
  contents: read

on:
  workflow_dispatch:
    inputs:
      release:
        type: choice
        description: 'Release type'
        default: 'patch'
        options:
          - 'patch'
          - 'minor'
          - 'major'
          - 'alpha'
          - 'beta'
      force:
        type: boolean
        default: false
        description: 'Force a stable release even if commits require a higher bump'

jobs:
  publish:
    if: github.actor == 'leanderpaul' && github.triggering_actor == 'leanderpaul' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ vars.SHADOW_MANAGER_APP_ID }}
          private-key: ${{ secrets.SHADOW_MANAGER_PRIVATE_KEY }}

      - name: Set up Node.js version
        uses: actions/setup-node@v4
        with:
          node-version: '24.x'
          registry-url: 'https://registry.npmjs.org'

      - name: Setup bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.14

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Release
        run: bunx shadow release ${{ github.event.inputs.release }} ${{ github.event.inputs.force == 'true' && '--force' || '' }}
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
